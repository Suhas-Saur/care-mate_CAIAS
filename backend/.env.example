# Backend Production Environment Variables
# Optional: Real Google Gemini API key for AI symptom assessment & diet generation
GEMINI_API_KEY=your_google_gemini_api_key_here

# Allowed origins for CORS (comma-separated or * for public access)
ALLOWED_ORIGINS=*

# Server port (automatically assigned by Render/Railway in production)
PORT=8000
